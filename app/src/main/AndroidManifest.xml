<?xml version="1.0" encoding="utf-8"?>

<manifest xmlns:android="http://schemas.android.com/apk/res/android"
  xmlns:tools="http://schemas.android.com/tools" >

  <uses-feature
    android:name="android.hardware.camera"
    android:required="false" />

  <uses-permission android:name="android.permission.INTERNET" />
  <uses-permission android:name="android.permission.CAMERA" />

  <application
    android:name=".PokedexApp"
    android:allowBackup="false"
    android:icon="@mipmap/ic_launcher"
    android:label="@string/app_name"
    android:supportsRtl="true"
    android:theme="@style/AppTheme"
    tools:ignore="AllowBackup">

    <activity
      android:name=".ui.main.MainActivity"
      android:exported="true"
      android:launchMode="singleTop">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />

        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>

    <activity
      android:name=".ui.details.DetailActivity"
      android:launchMode="singleTop" />

    <activity
      android:name=".type.ElementActivity"/>
    <activity
      android:name=".news.NewsActivity"/>


    <activity
      android:name=".type.TypeDetailsActivity"/>

    <activity
      android:name=".type.camera.CameraActivity"/>

    <activity
      android:name=".ui.details.PKMDActivity"/>

    <provider
      android:name="androidx.startup.InitializationProvider"
      android:authorities="me.sjihh.pokedex.androidx-startup"
      android:exported="false"
      tools:node="merge">

      <meta-data
        android:name="me.sjihh.pokedex.initializer.TimberInitializer"
        android:value="androidx.startup" />
    </provider>

    <profileable
      android:shell="true"
      tools:targetApi="q" />

  </application>

</manifest>